# DNAcloud Server 环境变量模板
# 复制此文件为 .env，填写所有 <REPLACE_ME> 项

# ─── 服务基础 ────────────────────────────────────────────────────────────────
SERVER_PORT=8080

# 服务公网地址（artifact 下载链接前缀，必须填写）
DNACLOUD_BASE_URL=https://<REPLACE_ME>

# ─── 数据库 ──────────────────────────────────────────────────────────────────
# 开发/演示用 H2（默认，无需额外配置）
# DB_URL=jdbc:h2:file:./data/dnacloud;DB_CLOSE_ON_EXIT=FALSE
# DB_DRIVER=org.h2.Driver
# DB_USERNAME=sa
# DB_PASSWORD=

# 生产用 MySQL（先运行 scripts/setup-mysql.sh）
# DB_URL=jdbc:mysql://127.0.0.1:3306/dnacloud?useSSL=false&serverTimezone=UTC&characterEncoding=utf8mb4
# DB_DRIVER=com.mysql.cj.jdbc.Driver
# DB_USERNAME=dnacloud
# DB_PASSWORD=<REPLACE_ME>
# JPA_DDL_AUTO=validate
# JPA_DIALECT=org.hibernate.dialect.MySQLDialect

# ─── Artifact 存储 ───────────────────────────────────────────────────────────
DNACLOUD_ARTIFACT_STORE=/opt/dnacloud/artifacts

# ─── OKX OnchainOS 支付凭证（服务端用于调用 OKX Broker API）─────────────────
# 获取地址：https://web3.okx.com/zh-hans/onchainos/dev-portal
# 注意：这是 OKX OnchainOS 开发者门户凭证，不是 OKX 交易所 API Key
OKX_API_KEY=<REPLACE_ME>
OKX_SECRET_KEY=<REPLACE_ME>
OKX_PASSPHRASE=<REPLACE_ME>

# OKX x402 Broker/Facilitator 地址（服务端调用 verify + settle）
OKX_X402_FACILITATOR_URL=https://www.okx.com/api/v5/onchainos/x402

# USDT 代币合约地址（XLayer mainnet）
USDT_CONTRACT_ADDRESS=<REPLACE_ME>

# ─── 平台钱包 ────────────────────────────────────────────────────────────────
# 平台收款地址（接收买家付款）
DNACLOUD_PAYMENT_ADDRESS=0x87939abdb04ef2d33f6eef9720b3759ed1441e41

# 平台出款私钥（可选；配置后 payout worker 将自动链上转账给创作者）
# 不配置时 payout 保持 pending 状态，需手动处理
# DNACLOUD_TREASURY_KEY=<REPLACE_ME>

# ─── 安全密钥 ────────────────────────────────────────────────────────────────
# Admin API Key（保护 /v1/creator/admin/* 端点，建议 32+ 字符随机字符串）
# 生成命令：openssl rand -hex 32
DNACLOUD_ADMIN_API_KEY=<REPLACE_ME>

# 包签名密钥（对上传 artifact 进行 HMAC 签名，建议 64+ 字符）
# 生成命令：openssl rand -hex 64
DNACLOUD_SIGNING_KEY=<REPLACE_ME>

# ─── 业务参数 ────────────────────────────────────────────────────────────────
# CORS 允许的前端域名（多个用逗号分隔）
DNACLOUD_CORS_ORIGINS=https://<REPLACE_ME>

# 平台分成比例（默认 20%）
DNACLOUD_PLATFORM_FEE_RATE=0.20

# 最小自动结算金额（单位：最小精度，默认 100000 = 0.1 USDT）
DNACLOUD_MINIMUM_PAYOUT=100000

# ─── 开发调试（生产必须保持注释或 false）────────────────────────────────────
# DNACLOUD_LOCAL_TEST=false    # true 时跳过支付验证，禁止在生产开启
